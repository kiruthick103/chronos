# TODO - Connect to Vercel

- [x] Inspect Vite/React project structure and build output
- [x] Add `vercel.json` at root for subdirectory build and SPA routing
- [x] Confirm Vercel project settings (repo root build command now in vercel.json)
- [ ] Deploy and verify SPA works on refresh/direct routes

